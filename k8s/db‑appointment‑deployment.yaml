apiVersion: apps/v1
kind: Deployment
metadata:
  name: db-appointment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db-appointment
  template:
    metadata:
      labels:
        app: db-appointment
    spec:
      containers:
        - name: postgres
          image: ${ECR_REGISTRY}/${REPO_NAME}:${IMAGE_TAG}
          env:
            - name: POSTGRES_DB
              value: AppointmentService
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: alwis
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: appointment-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: appointment-data
          hostPath:
            path: /data/db-appointment
            type: DirectoryOrCreate